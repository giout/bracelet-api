describe('Get all users', () => {
    test('Respond with 200', () => {

    })
    test('Return a JSON', () => {
        
    })
    test('Return an array', () => {
        
    })
    test('Return user properties', () => {

    })
})

describe('Create user', () => {
    test('Respond with 201', () => {

    })
    test('Return a JSON', () => {
        
    })
    test('User exists in database', () => {

    })
})

describe('Update user', () => {
    test('Respond with 200', () => {

    })
    test('Return a JSON', () => {
        
    })
    test('User is updated in database', () => {

    })
})

describe('Delete user', () => {
    test('Respond with 200', () => {

    })
    test('Return a JSON', () => {
        
    })
    test('User is removed from database', () => {
        
    })
})